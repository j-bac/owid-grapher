import { ClientSettings } from "./clientSettings"
import { ServerSettings } from "./serverSettings"

export const serverSettings: Partial<ServerSettings> = {
    // set overrides
}

export const clientSettings: Partial<ClientSettings> = {
    // set overrides
}
